git:
  overrideGpg: true
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}"
customCommands:
  - key: "n"
    command: 'wt -C "{{.SelectedWorktree.Path}}" switch --create --no-cd --base @ "{{index .PromptResponses 0}}"'
    context: "worktrees"
    description: "Worktrunk: create worktree from selected"
    output: terminal
    prompts:
      - type: input
        title: "New branch name"
        key: Branch
  - key: "<space>"
    command: 'wt switch --no-cd "{{.SelectedWorktree.Branch}}"'
    context: "worktrees"
    description: "Worktrunk: open in herdr"
    output: terminal
  - key: "W"
    command: 'wt switch --no-cd "{{.SelectedRemoteBranch.Name}}"'
    context: "remoteBranches"
    description: "Worktrunk: create worktree from remote branch"
    output: terminal
  - key: "d"
    command: 'wt remove --foreground "{{.SelectedWorktree.Path}}"'
    context: "worktrees"
    description: "Worktrunk: remove worktree"
    output: terminal
    prompts:
      - type: confirm
        title: "Remove worktree"
        body: "Remove '{{.SelectedWorktree.Branch}}'?"
